#include <stdio.h>
#include <cs50.h>

int main(void)
{
    long card_num;

    do
    {
        card_num = get_long("Number: ");
    } while (card_num < 1);

    //if (card_num)
    printf("%li\n", );
}

bool checksum(long card_num)
{
    for (int i = 10; c; i = i * 10)
    {
    }
}